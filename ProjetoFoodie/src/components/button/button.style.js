import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    btn: {
        width: "100%",
        backgroundColor: COLORS.red,
        borderRadius: 6
    },
    texto: {
        fontSize: FONT_SIZE.md,
        color: "white",
        padding: 14,
        textAlign: "center"
    }
}